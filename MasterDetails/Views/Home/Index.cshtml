@{

    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="panel panel-default">
    <div class="panel-heading">
        <div class="row">
            <h2 class="panel-title pull-left">
                <span>
                    Orders
                </span>
            </h2>
            <button class="btn btn-primary pull-right" id="addNewItem" data-toggle="modal" data-target="#newOrderModal" style="margin-right: 5px;">Add New</button>
        </div>
    </div>
    <div class="panel-body">
        <table class="table table-striped table-bordered" id="ordersTable">
            <thead>
                <tr>
                    <th>Customer Name</th>
                    <th>Address</th>
                    <th>Date</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody></tbody>
        </table>
    </div>

</div>

<div class="modal fade" id="newOrderModal" role="dialog" tabindex="-1">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button aria-label="Close" class="close" data-dismiss="modal" type="button"><span aria-hidden="true">×</span></button>


                <h4 class="modal-title">
                    New Order Information
                </h4>
            </div>
            <div class="modal-body">
                <h3>
                    Order Master
                </h3>
                <hr>
                <form id="customerForm">
                    <div class="form-horizontal">


                        <div class="form-group">
                            <label class="control-label col-md-2">
                                Customer Name
                            </label>


                            <div class="col-md-4">
                                <input class="form-control" id="customerName" placeholder="Customer Name" name="customerName" required type="text">
                            </div>
                            <label class="control-label col-md-2">
                                Address
                            </label>


                            <div class="col-md-4">
                                <input class="form-control" id="customerAddress" placeholder="Customer Address" name="Address" required type="text">
                            </div>
                            <input id="orderMasterId" type="hidden">
                        </div>
                    </div>
                </form>
                <h3 id="beforeError" style="margin-top: 10px;">
                    Order Details
                </h3>
                <table class="table" id="detailsTable">
                    <thead>
                        <tr>
                            <th><input class="selectall" style="width: 18%;" type="checkbox">Select All</th>
                            <th style="width: 21%;">Product</th>
                            <th style="width: 16%;">Unit Price</th>
                            <th style="width: 11%;">Quantity</th>
                            <th style="width: 18%;">Amount</th>
                            <th style="width: 8%;"><a href="#"  class="editDetail">Edit</a></th>
                            <th style="width: 8%;"><a  data-index="' + i + '"  href="#" class="deleteItem">Delete</a></th>
                        </tr>
                    </thead>
                    <tbody></tbody>
                    <tfoot>
                        <tr>
                            <td></td>
                            <td></td>
                            <td></td>
                            <td></td>
                            <td></td>
                            <td></td>
                            <td>
                                <a class="btn btn-info" @*data-target="#orderDetailsModal" data-toggle="modal"*@ id="addMore">Add Detail</a>
                            </td>
                        </tr>
                    </tfoot>
                </table>
            </div>
            <div class="modal-footer">
                <button class="btn btn-default closeModal" data-dismiss="modal"  type="button">Close</button>
                <button class="btn btn-primary" id="saveOrder" type="button">Save Order</button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="orderDetailsModal" role="dialog" tabindex="-1">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button aria-label="Close" class="close" data-dismiss="modal" type="button"><span aria-hidden="true">×</span></button>


                <h4 class="modal-title">
                    Order Details
                </h4>
            </div>
            <div class="modal-body">
                <div class="form-horizontal">
                    <div class="form-group">
                        <input id="detailId" type="hidden" />
                        <input id="index" type="hidden" />
                        <label class="control-label col-md-3">
                            Product Name
                        </label>


                        <div class="col-md-9">
                            <input class="form-control" id="productName" placeholder="Product Name" type="text">
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="control-label col-md-3">
                            Price
                        </label>


                        <div class="col-md-4">
                            <input class="form-control" id="price" placeholder="Product Price" type="number" >
                        </div>
                        <label class="control-label col-md-2">
                            Quanity
                        </label>


                        <div class="col-md-3">
                            <input class="form-control" id="quantity"  type="number">


                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-md-3 col-lg-offset-9">
                            <a class="btn btn-default" id="addToList">Add To List</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


@section scripts{

    <script>

        var itemList = [];

        $(function () {
            var orderUrl = '@Url.Action("getOrders","Home")';
            var table = $("#ordersTable").DataTable({
                "processing": true,
                "serverside": true,
                "filterng": false,
                "orderMulti": false,
                "ajax": {
                    "url": orderUrl,
                    "type": "POST",
                    "datatype": "json",


                },
                "columns": [
                    { "data": "customerName", "name": "customerName", "autowidth": true },
                    { "data": "address", "name": "address", "autowidth": true },
                    { "data": "orderDate", "name": "orderDate", "autowidth": true },
                    {
                        "data": "Edit", "name": "Edit", "autoWidth": true,
                        "render": function (data, type, row, meta) {
                            return '<a href="#" data-masterid=' + row.masterId + ' class="editOrder">Edit Order</a>'
                        }
                    },
                    {
                        "data": "Delete", "name": "Delete", "autoWidth": true,
                        "render": function (data, type, row, meta) {
                            return '<a href="@Url.Action("Index","Home")" data-masterid=' + row.masterId + ' class="deleteOrder">Delete</a>'
                        }
                    }

                ]
            });

        }),

        $("#addNewItem").click(function () {

            itemList = [];
                $("#orderMasterId").val(@Guid.Empty);
                $("#detailsTable tbody tr").remove();
                $("#saveOrder").html("Save Order");
                $(".error").remove();
                   clearItem();



        }),

        $("#addMore").click(function () {
                $("#index").val('');
                $("#productName").val('');
                $("#price").val('');
                $("#quantity").val('');
                $("#detailId").val(@Guid.Empty);
                $("#orderDetailsModal").modal('show');





        }),

        $("#addToList").click(function () {
                $(".error").remove();
                var Name = $("#productName").val();
                var Price = $("#price").val();
                var Quantity = $("#quantity").val();
                function valid() {

                     if($.trim(Name) == "") {
                        $("#productName").after('<span class="error">Product name is required</span>');
                        return false;

                    } else if (Name.length < 3) {
                        $("#productName").after('<span class="error">Product name is too short</span>');
                        return false;

                    } else if (Name.length > 10) {
                        $("#productName").after('<span class="error">Product name is too long</span>');
                        return false;

                    } if (Price == "") {
                        $("#price").after('<span class="error">Price is required</span>');
                        return false;
                    } else if (Price <= 0) {
                        $("#price").after('<span class="error">Price is must be greater than 0</span>');
                        return false;
                    } else if (Price > 9999999) {
                        $("#price").after('<span class="error">Invalid price (9,999,999 MAX)</span>');
                        return false;
                    }
                    if (Quantity == "") {
                        $("#quantity").after('<span class="error">Quantity is required</span>');
                        return false;
                    } else if (Quantity <= 0) {
                        $("#quantity").after('<span class="error">Quantity must be greater than 0</span>')
                        return false;
                    } else if (Quantity > 5000) {
                        $("#quantity").after('<span class="error">Please reduce Quantity (5000 Maximum)</span>')
                        return false;

                    }else {

                        return true;



                    }


                }
                if(valid()) {


                    var productName = $("#productName").val(),
                        price = $("#price").val(),
                        quantity = $("#quantity").val(),
                        index = $("#index").val();

                    if (index == "") {
                   
                        var data = {
                            "ProductName": productName,
                            "Amount": price,
                            "Quantity": quantity,
                            "index": itemList.length,
                         };

                        itemList.push(data);


                       clearOrderDetails();
                       function clearOrderDetails() {
                          $("#productName").val('');
                           $("#price").val('');
                           $("#quantity").val('');
                           


                          }

                        } else {
                    itemList[index] = {

                        "ProductName": productName,
                        "Amount": price,
                        "Quantity": quantity,

                    };

                    }
                    refreshItemlist();
                    clearOrderDetails();
                    function clearOrderDetails() {
                        $("#productName").val('');
                        $("#price").val('');
                        $("#quantity").val('');
                       


                    }
                    $("#orderDetailsModal").modal("hide");

                  }
        }),

        $(document).on("click", '.deleteOrder', function (e) {
                var id = $(this).attr("data-masterid");
                var Url = '@Url.Action("deleteOrder","Home", new { area = "" })';
                            return $.ajax({
                                type: 'POST', 
                                url: Url,
                                data: "id=" + id

                            });

            }),

        $(document).on('click', 'a.deleteItem', function (e) {
            e.preventDefault();
            $("input:checked").each(function () {
                index = $(this).val();
                itemList.splice(index, 1);

            });
               

               
                   
                refreshItemlist();
                $("#newOrderModal").modal("handleUpdate");
                 
               
            }),

      

            $(document).on("click", '.editOrder', function (e) {
                itemList = [];
                $(".error").remove();
                var data = $(this).data();
                e.preventDefault();
                var id = data.masterid;

                $.when(getSingleOrder(id)).then(function (res) {

                    $("#customerName").val(res.CustomerName);
                    $("#customerAddress").val(res.Address);
                    $("#orderMasterId").val(res.MasterId);

                    itemList = [];
                    $.each(res.OrderDetails, function (i, v) {
                        var data = {
                            "ProductName": v.ProductName,
                            "Amount": v.Amount,
                            "Quantity": v.Quantity,
                            "index": i,
                            "MasterId": v.MasterId,
                            "State": "Saved",
                            
                        };

                        itemList.push(data);

                    });
                    refreshItemlist();
                    $('#newOrderModal').modal('show');


                    $("#saveOrder").html("Save Update");


                }).fail(function (err) {
                    console.log(err);
                });
        })

       
        $('.selectall').change(function () {
            $('input:checkbox').prop('checked', $(this).prop('checked'));


        })
        $(document).on("click", '.checkbox', function () {

            if ($(this).prop("checked") == false) {
                $('.selectall').prop("checked", false)
            }
          
        }),


        $(document).on("click", '.editDetail', function (e) {
             e.preventDefault;
            var index;
            $("input:checked").each(function () {
                index = $(this).val();  
               
               
            });
            $.when(getFromItemList(index)).then(function (order) {

                $("#productName").val(order.ProductName);
                $("#price").val(order.Amount);
                $("#quantity").val(order.Quantity);
                $("#index").val(order.index);
                $("#orderDetailsModal").modal('show');

            });
        });

        function getOrder(id) {
            return $.ajax({
                type: 'GET',
                url: '@Url.Action("getSingleOrder", "Home", new { area = "" })',
                data: "orderId=" + id
            });
        }

        function getSingleOrder(id) {
            return $.ajax({
                type: 'GET',
                url: '@Url.Action("getSingleOrder", "Home", new { area = "" })', 
                data: "orderId=" + id
            });
        }

        function clearItem() {
        $("#productName").val('');
        $("#price").val('');
        $("#quantity").val('');
        $("#customerName").val('');
        $("#customerAddress").val('');
        $("#orderMasterId").val('');
        }

        function getSingleOrderDetail(id) {
            return $.ajax({
                type: 'GET',
                url: '@Url.Action("getSingleOrderDetail", "Home", new { area = "" })', 
                data: "orderId=" + id
            });

        }

        function updateSingleOrderDetail(data) {
                $.ajax({

                url: '@Url.Action("updateSingleOrderDetail", "Home", new { area = "" })',
                contentType: 'application/json; charset=utf-8',
                dataType: 'json',
                type: 'POST',
                data: data


            });

        }

        function getFromItemList(index) {
            return itemList[index];
        }

        function refreshItemlist() {
            $("#detailsTable tbody").empty();
            if (itemList.length > 0) {

                for (i = 0; i < itemList.length; i++) {
                    var itemRow = '<tr><td><input class="checkbox" value="' + itemList[i].index + '" type="checkbox"><td>' + itemList[i].ProductName + '</td><td>' + itemList[i].Amount + '</td><td>' + itemList[i].Quantity + '</td><td>' + (parseFloat(itemList[i].Amount) * parseInt(itemList[i].Quantity)) + '</td></tr>';

                    $("#detailsTable tbody").append(itemRow);


                }
            }




        }

        $("#saveOrder").click(function (e) {
            e.preventDefault();
            $(".error").remove();
            var Name = $('#customerName').val();
            var Address = $('#customerAddress').val();
            function valid() {
            if (Name.length < 1) {
                $('#customerName').after('<span class="error">Customer Name is required</span>');
                return false;

            } else if (Name.length < 3) {
                $('#customerName').after('<span class="error">Customer Name is too short</span>');
                return false;

            }
            if (Name.length > 15) {

                $('#customerName').after('<span class="error">Customer Name is too long</span>');
                return false;
            }
            if (Address.length < 1) {
                $('#customerAddress').after('<span class="error">Address is required</span>');
                return false;

            } else if (Address.length < 7) {
                $('#customerAddress').after('<span class="error">Address is too short</span>');
                return false;
            } if (Address.length > 30) {
                $('#customerAddress').after('<span class="error">Address is too long</span>');
                return false;

            } else {
                return true;
            }
        }
        if (valid()) {
                    
            if ( itemList.length > 0) {
                var data = JSON.stringify({
                    MasterId: $("#orderMasterId").val(),
                    CustomerName: $("#customerName").val(),
                    Address: $("#customerAddress").val(),
                    OrderDetails: itemList
                });

                $.when(saveOrder(data)).then(function (response) {
                    var homeUrl = '@Url.Action("Index","Home")';
                    window.location.href = homeUrl;
                    console.log(response);
                }).fail(function (err) {
                    console.log(err);
                });
                function saveOrder(data) {

                    var saveUrl = '@Url.Action("saveOrder", "Home", new { area=""})';
                    return $.ajax({
                        contentType: 'application/json; charset=utf-8',
                        dataType: 'json',
                        type: 'POST',
                        url: saveUrl,
                        data: data
                    });


                }
            } else {
                $("#beforeError").after('<h4 class="error">Please Add Order Details</h4>');
            }
        }



        })

    </script>

    }



